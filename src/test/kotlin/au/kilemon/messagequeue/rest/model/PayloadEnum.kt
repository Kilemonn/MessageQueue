package au.kilemon.messagequeue.rest.model

/**
 * An enum class to wrap in an object and use in tests to verify serialisation to and from JSON.
 *
 * @author github.com/Kilemonn
 */
enum class PayloadEnum
{
    A, B, C;
}
